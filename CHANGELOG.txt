--- browserOS Version 0.1.x.x ---
v0.1:
- THE first release of browserOS.

v0.1.5:
- Includes a crash handling system to prevent sudden crashes.

v0.1.5.2:
- Fixes a few bugs with the crash handler.

v0.1.7:
- Adds 5 new commands: rename, credits -py, copyright [-py], and warrenty.
- Removes the redundant "list -tree" command from help.

v0.1.7.5:
- Adds more troubleshooting parameters to the "crash.txt" file.

v0.1.9:
- Adds 6 new mathmatical commands: add, sub, div, mult, pos, and neg.
- Commands "del -unload" and "reload" are available. 

v0.1.9.5:
- Cleans up unessesary boot-time loading, such as the external function loader.
- Fixes a few typos.
- Removes faulty "store -tree" command.
- Becomes final version of Version 0.1 and only prioitized version.

v0.1.9.5-r2:
- Fixes minor unknown bugs.

v0.1.9.5-r3:
- Fixes a major bug when taking an input for writing a file.
- "About" info lists v0.1.9.5 as a LTS release.
- Removes unessesary condition on Line 475.
- Shortended the repetitive try/except clauses in the beginning.
- Added the information blurb about v0.2 commands in the help page.
- Removed the X-ed out commands that seems to never be available.
- Changed the license and warrenty paragraphs to be more in line with the new license.
- Shortened the else clause in readFiles().

--- browserOS Version 0.2.x ---
0.2-snapshot-1:
- First preview release of version 0.2.

0.2-snapshot-2:
- Adds a notice for older users on the old commands.

0.2-snapshot-3:
- Adds the commands "shutdown" and upcoming "exit" to the help page.

0.2-snapshot-4:
- Adds the new (future) command "define."
- Command "shutdown" will be omitted.
- Command "list" now has a different way of listing files.
- "Stupid restart" is now an easter egg for EOFError.
- The info command now prints the storage amounts and CPU cores in an fstring, which omits the str() function.
- Line 453: "shutdown" is now a command that will trigger the reminder "I think you mean 'exit'".
- cat() and readf() now display the size of a file.
- BrowserOS is now released under the Apache License.
- The "start" command now displays the old version warning.
- Fixed bug where even entering 'No' in the overwrite dialog, it would still write.
- In the info() function, make a different way of displaying size.
- Adds another new command "verify."
- Added "copyright" to the boot-up splash info.
- Added more new commands: "date" and "time".
- ^ Which adds more short functions: timef() and timedf().
- ^ And also a help describer when using "help".
- Removed unnessesary indents for REPL interpreter.
- A new command was added to replace the load command: register and register -file.

0.2-snapshot-5:
- The registerf() command now works properly.
- readxf() and readyf() prints contents of the X and Y registers.
- swapreg() is now a new function for the register class.
^ clearreg() is also a new function.
- This version also adds new commands for each function.
- writex() and writey() are new functions for the register class.
^ There are also commands for writing the registers' data.
- Removed the unessesary crash.txt generator when it catches an EOFError or KeyboardInterrupt.
- Removed the other clause for the "info" command.
- Added a new function to get raw files from GitHub.
^ Along with a new command.
- If the directory is not specified, it will now ask you to put a directory.
- User can now delete plank.py, but not browseros_v0.2-snapshot-5.py.
- Every function now returns a 1 or 0 condition.
- REPL loop now uses a match case statement.
- Changed grammar errors in renamef() and clonef().
- browserOS now doesn't use shutil.
- Organized the library imports to two lines.
^ Which gets rid of the "requests" library to make browserOS more portable.
- listf() now uses listdir() instead of walk().
- Crash writer now uses proper placeholders for formatting.
- Removed the IP address indicator.
- 
